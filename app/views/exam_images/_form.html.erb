<%= form_for(@exam_image) do |f| %>
  <% if @exam_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam_image.errors.count, "error") %> prohibited this exam_image from being saved:</h2>

      <ul>
      <% @exam_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :filename %><br>
    <%= f.text_field :filename %>
  </div>
  <div class="field">
    <%= f.label :width %><br>
    <%= f.number_field :width %>
  </div>
  <div class="field">
    <%= f.label :height %><br>
    <%= f.number_field :height %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
