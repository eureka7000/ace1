<%= form_for(@user_relation) do |f| %>
  <% if @user_relation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_relation.errors.count, "error") %> prohibited this user_relation from being saved:</h2>

      <ul>
      <% @user_relation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :related_user_id %><br>
    <%= f.number_field :related_user_id %>
  </div>
  <div class="field">
    <%= f.label :relation_type %><br>
    <%= f.text_field :relation_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
